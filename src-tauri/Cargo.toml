[package]
name = "gravitation-cubed"
version = "2.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

physics-engine = { path = "../crates/physics-engine" }

[build-dependencies]
tauri-build = { version = "2", features = [] }

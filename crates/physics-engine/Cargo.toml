[package]
name = "physics-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nalgebra = { version = "0.33", features = ["serde-serialize"] }
rand = "0.8"

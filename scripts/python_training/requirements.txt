# Python Training Requirements - Simplified for compatibility
# Core ML/DL packages

# Core dependencies
tensorflow==2.20.0           # TensorFlow (latest stable)
keras==3.12.0               # Keras 3.x (compatible with TF 2.20)
numpy==1.26.4               # Compatible with TF 2.20
pandas==2.1.4               # Data manipulation

# Visualization and ML utilities
matplotlib==3.8.2           # Visualization
scikit-learn==1.3.2         # Additional ML tools

# Note: jupyter and tensorflowjs removed due to dependency conflicts
# Install separately if needed:
# pip install jupyter
# pip install tensorflowjs (requires older TF version)
